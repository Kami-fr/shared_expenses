/**
 * Entry point of the Shared Expenses panel.
 *
 * Home Assistant loads the built bundle as a single ES module and instantiates
 * the `shared-expenses-panel` custom element.
 */

export * from "./shared-expenses-app";
